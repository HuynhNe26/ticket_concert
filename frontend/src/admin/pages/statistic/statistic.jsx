import React, { useState } from "react";

export default function StatisticPage () {
    const [hotSearch, setHotSearch] = useState([]);
    const [users, setUsers] = useState([]);
    
    return (
        <div>Trang thống kê sự kiện</div>
    )
}